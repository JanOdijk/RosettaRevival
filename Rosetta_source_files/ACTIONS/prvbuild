echo "build: $1:$2"
if ( test "$1" = "dutch") || (test "$1" = "english") || (test "$1" = "spanish")
then make -n -f $1/descrip.make $1/target/$2 language=$1
else make -n -f dutch/descrip.make $1/target/$2 language=dutch
fi
