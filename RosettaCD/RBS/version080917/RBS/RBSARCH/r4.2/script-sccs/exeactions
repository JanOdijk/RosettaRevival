case $# in
0) ;;
*) echo "Usage: exeactions"; exit;;
esac
savedir=`pwd`
cd $RBS_USER_ROOT$RBS_USER$RBS_RELEASE
#
chmod u+x actions_.rbs
#
set -e
trap "cd $savedir; exit 1" 0
actions_.rbs 
#
cd $savedir
trap 0
