echo "inputfile    general/mopt.make" > lognames
echo "language     $1" >> lognames
echo "target       $2.exe" >> lognames
cat RBS_logicals >> lognames
if test -s $1/target/$2.opt
   then  rm -f $1/target/$2.opt
fi
execute tools mopttoopt.exe > $1/target/$2.opt

